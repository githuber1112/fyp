import React from 'react';
import './styles.scss';

const Admin = props => {
    return(
        <diV>
            <h1>
                Admin
            </h1>
        </diV>
    );
};

export default Admin;